import {CircularProgress, CircularProgressProps} from "@mui/material"
import {styled} from "@mui/material/styles"

export const MyLoader = styled((props: CircularProgressProps) => <CircularProgress {...props} />)(({theme}) => ({}))
